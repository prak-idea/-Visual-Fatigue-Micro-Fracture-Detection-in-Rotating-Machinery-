 Why this is a Unique Contribution
While standard PdM uses sensor images (converting 1D signals to 2D spectrograms), you are using raw video of mechanics. You will be solving the "Temporal Domain Shift" problem where the model struggles to adapt to a new machine with different rotational speeds. The model must differentiate between intended motion (rotation) and unintended motion (failure).
